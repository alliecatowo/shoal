[package]
name = "shoal-proto"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
proptest = "1.7"
